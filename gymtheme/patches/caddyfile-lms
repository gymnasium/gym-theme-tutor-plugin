# via gym-theme caddyfile-lms
header * {
    Content-Security-Policy "
        connect-src {{ LMS_HOST }} *.{{ LMS_HOST }} {{ BASE_DOMAIN }} *.{{ BASE_DOMAIN }} {{ MARKETING_SITE_BASE_URL }};
    "
}
