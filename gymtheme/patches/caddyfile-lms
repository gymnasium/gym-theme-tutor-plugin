# via gym-theme caddyfile-lms
header * {
    Content-Security-Policy "
        default-src 'self' {{ LMS_HOST }} *.{{ LMS_HOST }} {{ CMS_HOST }} *.{{ CMS_HOST }} {{ BASE_DOMAIN }} *.{{ BASE_DOMAIN }} {{ MARKETING_SITE_BASE_URL }};
        connect-src {{ LMS_HOST }} *.{{ LMS_HOST }} {{ CMS_HOST }} *.{{ CMS_HOST }} {{ BASE_DOMAIN }} *.{{ BASE_DOMAIN }} {{ MARKETING_SITE_BASE_URL }};
    "
}
